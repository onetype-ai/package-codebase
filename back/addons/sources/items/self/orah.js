onetype.AddonReady('codebase.sources', (sources) =>
{
	sources.Item({
		id: 'orah',
		name: 'Orah',
		package: '@onetype/orah',
		description: 'The mind of the instance. A conversational orchestrator that plans across specialized agents — each agent isolated with its own instructions and tools, reached only through research and task calls.'
	});
});
